<!--theme.wxml-->
<view class="container">
    <navigator class="card" url="../list/list?id={{item.sportId}}&title={{item.sportName}}"  wx:for="{{list}}">
        <image src="{{item.display}}" class="card-image"></image>
        <view class="card-content">
            <view>
                <text class="card-title">{{item.sportName}}</text>
            </view>
            <view>
                <text class="card-desc">{{item.description}}</text>
            </view>
        </view>
    </navigator>
</view>